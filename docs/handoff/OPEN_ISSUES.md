# Open Issues

Statuses are machine-oriented: `blocked` prevents the next named task; `pending` identifies work whose prerequisites are not yet satisfied. Exit gates require reproducible evidence and independent review, not a prose assertion.

| ID | Status | Evidence | Impact | Exit gate | Forbidden claim |
|---|---|---|---|---|---|
| `MIGRATION-ENV-2025-2` | resolved | `build/metadata/environment_ready.json` is `ready:true` for Vivado 2025.2 build `6299465`, Python 3.12.13, a clean Git tree, and unchanged authority hashes. | The current machine may produce current-machine catalog, RFDC, and connected-shell evidence. | Preserve the environment manifest binding and rerun it whenever the Git commit or tool installation changes. | “Evidence is portable without its environment manifest.” |
| `BD-T5-REPORT-PROTOCOL` | resolved | The latest fresh real Vivado 2025.2 attempt passed the bounded report parser and atomically published success. The evidence binds request, realization Tcl, verification Tcl, readback, CDC, clock, timing, and utilization hashes. | The connected-shell protocol is structurally authoritative within its OOC timing scope. | Keep the parser fail-closed and regenerate evidence after any source, authority, or tool change. | “Post-route timing or production integration is complete.” |
| `BD-T5-MTS-AUTHORITY` | resolved | Fresh RFDC probe evidence records the measured MTS properties and six tile bindings; the connected request consumes the probe provenance and the OOC run confirms MTS configuration readback. | Configuration is proven; runtime SYSREF alignment and deterministic phase/latency remain open. | Preserve the measured property names/values and complete runtime MTS evidence later. | “MTS runtime is verified” or “deterministic latency is accepted.” |
| `GEM3-BOARD-IO` | pending | The SHA-bound legacy BD has ENET3 and MDIO disabled; schematic audit alone does not freeze enabled PS/PHY integration. | Ethernet production wiring, reset, PHY address, and delay policy lack one enabled board authority. | Bind a reviewed board source for MIO64-77, reset, PHY address, RGMII delay/link mode and electrical bank; add negative tests and Vivado readback. | “GEM3 board wiring accepted” or “Ethernet ready”. |
| `PRODUCTION-2SPC` | pending | Current generated ingress/TX boundary RTL is legacy reference only; production responsibilities remain architecture pending. | The continuous 500 MSPS reflection chain has no production ingress/egress implementation. | Implement Cycle-owned RX/TX 2SPC boundaries, generate RTL, pass bit/cycle equivalence, backpressure/gap/reset tests, synthesis, and review. | “Legacy 2SPC satisfies production”. |
| `MONITOR-EVENT-CHAIN` | pending | Golden detector/event semantics exist, but complete Cycle hardware, queues, and generated RTL do not. | No production pulse-only IQ/PDW event stream exists. | Implement and verify FIR decimation, power/noise/vote/refinement, IQ capture, range association, ordering, overflow and packet framing without RFDC backpressure. | “Pulse events are hardware-ready”. |
| `EVENT-DMA` | pending | DMA is catalog-locked but not instantiated or connected to event queues/DDR. | Hit IQ and PDW records cannot reach software memory. | Add S2MM topology, address map, descriptors/ring ownership, overflow/error status, tests, synthesis and software readback. | “DMA transport integrated”. |
| `GEM3-DATA-PLANE` | pending | GEM3 board authority and event DMA are open. | No event-only network transfer exists. | Close `GEM3-BOARD-IO` and `EVENT-DMA`; integrate software packetization, packet sequence/loss accounting, throughput and recovery tests. | “Network upload works”. |
| `CDC-TIMING` | pending | The OOC connected run is structurally ready: exact AMD CDC-11/13/15 waiver endpoint sets are present, synthesis has 0 errors/warnings, and timing is explicitly scoped to `ooc_boundary_only` with 515 missing input delays and 536 missing output delays. | Post-route timing and full-top-level CDC remain unaccepted; these OOC boundary counts are not implementation failures. | Integrate the data path and close implementation timing/CDC at the real top level without broad waivers. | “Post-route timing closed”, “all CDC clean”, or “250 MHz production-ready.” |
| `MTS-RUNTIME` | pending | RFDC properties are probed, but runtime driver/SYSREF synchronization is not. | Multi-tile phase and deterministic latency are unknown. | Exact MTS config authority, runtime driver success, SYSREF evidence, repeated phase/latency measurements and reset-cycle stability. | “Coherent 8-channel phase” or “deterministic latency”. |
| `BOARD-LOOPBACK` | pending | No accepted end-to-end 8 ADC/8 DAC RF measurement exists. | Physical H/V/range identity and long-term RF performance remain unproved. | Measure continuity, H/V identity, +20/0/-20 dB ratios, DAC-to-ADC loopback, phase, clipping, pulse event transport and 24-hour stability. | “Board accepted”, “all channels verified”, or “24-hour stable”. |
